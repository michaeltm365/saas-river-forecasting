# Observed-period dry counts

Raw seed-42 availability RGCN probabilities, without calibration or a rho cap. Counts include only genuine sensor-observed validation dates. Nominal 95% simulation intervals include the observed count at 18 of 22 reaches. These are retrospective aggregates of rolling three-day forecasts.

Regenerate with `python benchmarks/paper_canonical_report.py`.
